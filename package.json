{
  "name": "lacman-online",
  "version": "1.0.0",
  "description": "Multiplayer Lacman Game",
  "main": "server.js",
  "scripts": {
    "start": "node server",
    "client": "npm start --prefix client",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix client && npm run build --prefix client"
  },
  "dependencies": {
    "bootstrap": "^5.0.0",
    "express": "^4.17.1",
    "socket.io": "^3.1.1"
  }
}
